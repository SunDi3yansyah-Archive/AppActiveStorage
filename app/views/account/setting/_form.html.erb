<%= form_for(account_setting, url: account_update_setting_path, local: true) do |form| %>
  <% if account_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_setting.errors.count, "error") %> prohibited this account setting from being saved:</h2>

      <ul>
      <% account_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>


  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
